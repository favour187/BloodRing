# Server

Dedicated game-server configuration: authoritative match simulation tick, zone/storm
authority, spawn systems, and per-region server pool definitions. The server consumes
the same data-driven configs as the client to stay in lockstep.


